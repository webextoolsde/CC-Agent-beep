import { AgentContactProps } from "./PanelProperties";
export declare class VisibilityChecker {
    agentContactProps?: AgentContactProps;
    checkVisibility: (tag: string, upd: (visible: boolean) => void) => {
        visible: boolean;
        dispose: import("mobx").IReactionDisposer;
    } | {
        visible: boolean;
        dispose?: undefined;
    };
}
