import { DateTime } from "luxon";
/**
 * Gets current server time as a Luxon DateTime object
 * @param fallback - DateTime to use if server time is unavailable
 * @returns DateTime object representing server time or fallback
 */
export declare function getCurrentServerTime(fallback: DateTime): DateTime;
/**
 * Gets current server time as milliseconds since epoch
 * @param fallback - Milliseconds to use if server time is unavailable
 * @returns Number of milliseconds representing server time or fallback
 */
export declare function getCurrentServerTimeInMillis(fallback: number): number;
