import { Service } from "../..";
export type DeviceInfo = {
    id: string;
    name: string;
    type: string;
    available?: boolean;
    isPreferredAnswerEndpoint: boolean;
};
export type DeviceMap = Record<string, DeviceInfo[]>;
export declare enum LineOwnerType {
    PEOPLE = "PEOPLE",
    PLACE = "PLACE",
    VIRTUAL_LINE = "VIRTUAL_LINE",
    UNKNOWN = "UNKNOWN"
}
export declare enum ExtensionType {
    PRIMARY = "PRIMARY",
    SECONDARY = "SECONDARY",
    UNKNOWN = "UNKNOWN"
}
export type ExtensionTypeInfo = {
    type: ExtensionType;
    lineOwnerType: LineOwnerType;
    lineOwnerId: string;
    preferredAnsweringEndPointId: string;
};
export type ExtensionTypeInfoMap = Record<string, ExtensionTypeInfo>;
export type WebexDnExtensionData = {
    dialNumbers: string[];
    extensions: string[];
    devicesMap: DeviceMap;
    extensionTypeInfoMap: ExtensionTypeInfoMap;
    directNumberDevicesMap: DeviceMap;
};
export declare function parseContactCenterExtensions(ccExtensions: Service.WebexCalling.ContactCenterExtensions): WebexDnExtensionData;
